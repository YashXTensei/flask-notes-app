# 📝 Flask Notes App

A simple personal notes manager built using **Flask** and **SQLite**.
Users can create, view, edit, and delete notes with timestamps.

This project was built as a learning project during my **2nd semester**
while learning Flask.

---

## 🚀 Features

- Add new notes
- Edit existing notes
- Delete notes (with confirmation)
- View all notes or a single expanded note
- SQLite database (persistent storage)
- Timestamp for each note (created / updated)
- Basic CSS for clean UI

---

## 🛠️ Tech Stack

- Python
- Flask
- SQLite
- HTML & CSS

---

## 📂 Project Structure

- `app.py` – main Flask application
- `templates/` – HTML templates
- `static/` – CSS files
- `Project2.db` – SQLite database (generated automatically)

---

## ⚙️ How to Run Locally

```bash
git clone https://github.com/YashXTensei/flask-notes-app.git
cd flask-notes-app
python app.py
